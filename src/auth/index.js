import reducer from "./reducer";
import * as types from "./actionTypes";
import * as selectors from "./selectors";
import * as constants from "./constants";

export default { reducer, types, selectors, constants };
